services:
  web:
    volumes:
      - type: bind
        source: ./
        target: /app
  s3_poller:
    volumes:
      - type: bind
        source: ./
        target: /app
  worker:
    volumes:
      - type: bind
        source: ./
        target: /app
